version: '3'
services:
  app:
    image: cosemi
    container_name: cosemi-container
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_RUN_ON_SAVE=true
      - WATCHDOG_TIMEOUT=10
